node_name: bmg_billy
node_id: 01KAC1JS0HYKAQDD39PKYN36S4
node_description: A node definition for BMG plate reader
node_type: device
module_name: bmg_module
module_version: 1.1.0
capabilities:
  get_info: true
  get_state: true
  get_status: true
  send_action: true
  get_action_status: true
  get_action_result: true
  get_action_history: true
  action_files: true
  send_admin_commands: true
  set_config: true
  get_resources: false
  get_log: true
  admin_commands:
  - lock
  - reset
  - shutdown
  - unlock
node_url: null
actions:
  open:
    name: open
    description: Opens the BMG plate tray.
    args: {}
    locations: {}
    files: {}
    results: {}
    blocking: true
    asynchronous: true
    accepts_var_args: false
    accepts_var_kwargs: false
    var_args_schema: null
    var_kwargs_schema: null
  close:
    name: close
    description: Closes the BMG plate tray.
    args: {}
    locations: {}
    files: {}
    results: {}
    blocking: true
    asynchronous: true
    accepts_var_args: false
    accepts_var_kwargs: false
    var_args_schema: null
    var_kwargs_schema: null
  set_temp:
    name: set_temp
    description: Sets the temperature on the BMG microplate reader.
    args:
      temp:
        name: temp
        description: Temperature in Celcius. Valid options are 0.0, 0.1, or 25.0 through
          45.0 (10.0 through 60.0 for extended temperature range models).
        argument_type: float
        required: true
        default: null
    locations: {}
    files: {}
    results: {}
    blocking: true
    asynchronous: true
    accepts_var_args: false
    accepts_var_kwargs: false
    var_args_schema: null
    var_kwargs_schema: null
  run_assay:
    name: run_assay
    description: Runs an assay on the BMG plate reader
    args:
      assay_name:
        name: assay_name
        description: ''
        argument_type: str
        required: true
        default: null
      data_output_directory_path:
        name: data_output_directory_path
        description: Data output directory path. Path must point to an existing folder.
          Defaults to 'C:\Program Files (x86)\BMG\CLARIOstar\User\Data'
        argument_type: str
        required: false
        default: null
      data_output_file_name:
        name: data_output_file_name
        description: Data output file name (ex. data.txt). Will default to <timestamp>.txt
          (ex. 1731706249.txt) if no file name is entered.
        argument_type: str
        required: false
        default: null
    locations: {}
    files: {}
    results:
      file:
        result_label: file
        description: null
        result_type: file
      json_result:
        result_label: json_result
        description: null
        result_type: json
        json_schema:
          properties:
            data:
              title: Data
              type: string
          required:
          - data
          title: strModel
          type: object
    blocking: true
    asynchronous: true
    accepts_var_args: false
    accepts_var_kwargs: false
    var_args_schema: null
    var_kwargs_schema: null
config:
  node_definition: C:\\Users\\RPL\\source\\repos\\bmg_module\\definitions\\bmg_billy.node.yaml
  node_info_path: null
  update_node_files: true
  status_update_interval: 2.0
  state_update_interval: 5.0
  node_url: http://suestorm.cels.anl.gov:3003/
  uvicorn_kwargs:
    limit_concurrency: 10
  enable_rate_limiting: true
  rate_limit_requests: 100
  rate_limit_window: 60
  rate_limit_short_requests: 50
  rate_limit_short_window: 1
  rate_limit_cleanup_interval: 300
  data_output_directory_path: C:\Program Files (x86)\BMG\CLARIOstar\User\Data
  db_directory_path: C:\Program Files (x86)\BMG\CLARIOstar\User\Definit
  extended_temperature_range_model: false
config_schema:
  description: Configuration for the BMG node.
  properties:
    node_definition:
      anyOf:
      - type: string
      - format: path
        type: string
      - type: 'null'
      default: default.node.yaml
      description: Path to the node definition file to use. If set, the node will
        load the definition from this file on startup. Otherwise, a default configuration
        will be created.
      title: Node Definition File
    node_info_path:
      anyOf:
      - type: string
      - format: path
        type: string
      - type: 'null'
      default: null
      description: Path to export the generated node info file. If not set, will use
        the node name and the node_definition's path.
      title: Node Info Path
    update_node_files:
      default: true
      description: Whether to update the node definition and info files on startup.
        If set to False, the node will not update the files even if they are out of
        date.
      title: Update Node Files
      type: boolean
    status_update_interval:
      anyOf:
      - type: number
      - type: 'null'
      default: 2.0
      description: The interval in seconds at which the node should update its status.
      title: Status Update Interval
    state_update_interval:
      anyOf:
      - type: number
      - type: 'null'
      default: 5.0
      title: State Update Interval
    node_url:
      default: http://127.0.0.1:2000/
      description: The URL used to communicate with the node. This is the base URL
        for the REST API.
      format: uri
      minLength: 1
      title: Node URL
      type: string
    uvicorn_kwargs:
      additionalProperties: true
      description: Configuration for the Uvicorn server that runs the REST API. By
        default, sets limit_concurrency=10 to protect against connection exhaustion
        attacks.
      title: Uvicorn Configuration
      type: object
    enable_rate_limiting:
      default: true
      description: Enable rate limiting middleware for the REST API.
      title: Enable Rate Limiting
      type: boolean
    rate_limit_requests:
      default: 100
      description: Maximum number of requests allowed per long time window (only used
        if enable_rate_limiting is True).
      minimum: 1
      title: Rate Limit Requests
      type: integer
    rate_limit_window:
      default: 60
      description: Long time window in seconds for rate limiting (only used if enable_rate_limiting
        is True).
      minimum: 1
      title: Rate Limit Window
      type: integer
    rate_limit_short_requests:
      anyOf:
      - minimum: 1
        type: integer
      - type: 'null'
      default: 50
      description: Maximum number of requests allowed per short time window for burst
        protection (only used if enable_rate_limiting is True). If None, short window
        limiting is disabled.
      title: Rate Limit Short Requests
    rate_limit_short_window:
      anyOf:
      - minimum: 1
        type: integer
      - type: 'null'
      default: 1
      description: Short time window for burst protection in seconds (only used if
        enable_rate_limiting is True). If None, short window limiting is disabled.
      title: Rate Limit Short Window
    rate_limit_cleanup_interval:
      default: 300
      description: Interval in seconds between cleanup operations to prevent memory
        leaks (only used if enable_rate_limiting is True).
      minimum: 1
      title: Rate Limit Cleanup Interval
      type: integer
    data_output_directory_path:
      default: C:\Program Files (x86)\BMG\CLARIOstar\User\Data
      format: path
      title: Data Output Directory Path
      type: string
    db_directory_path:
      default: C:\Program Files (x86)\BMG\CLARIOstar\User\Definit
      format: path
      title: Db Directory Path
      type: string
    extended_temperature_range_model:
      default: false
      title: Extended Temperature Range Model
      type: boolean
  title: BMGNodeConfig
  type: object
