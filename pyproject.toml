[project]
name = "bmg_module"
version = "1.1.0"
description = "Software for automatting a BMG VANTAstar"
authors = [
    {name = "Casey Stone", email="cstone@anl.gov"},
    {name = "Ryan D. Lewis", email="ryan.lewis@anl.gov"},
]

dependencies = [
    "madsci.node_module~=0.5.0",
    "madsci.client~=0.5.0",
    "madsci.common~=0.5.0",
    "pywin32",
    "comtypes",
]

requires-python = ">=3.9.1"
readme = "README.md"
license = {text = "MIT"}

[project.urls]
homepage = "https://github.com/AD-SDL/brooks_xpeel_module"

######################
# Build Info + Tools #
######################
[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
    "ruff>=0.11.7",
    "pytest",
]
